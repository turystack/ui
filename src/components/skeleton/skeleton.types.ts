import type { ComponentConfig } from '@/support/types'

export type SkeletonSlots = 'root'

export type SkeletonProps = {
	className?: string
}

export type SkeletonConfig = ComponentConfig<SkeletonProps, SkeletonSlots>
